{
  "name": "fuxicun-website",
  "version": "1.0.0",
  "description": "福溪村官方网站 - Cloudflare Workers + D1 + R2 + KV",
  "private": true,
  "scripts": {
    "build": "wrangler pages functions build --outdir=./dist/worker/",
    "dev": "npm run build && wrangler dev --config wrangler.jsonc --persist-to ../.wrangler-state-fuxicun",
    "dev:pages": "wrangler pages dev",
    "dev:clean": "node -e \"require('fs').rmSync('.wrangler',{recursive:true,force:true})\" && npm run dev",
    "deploy": "npm run build && wrangler deploy --config wrangler.jsonc",
    "deploy:pages": "wrangler pages deploy"
  },
  "devDependencies": {
    "puppeteer-core": "^25.0.4",
    "wrangler": "^4.98.0"
  },
  "dependencies": {
    "@cloudflare/workerd-windows-64": "^1.20260523.1"
  }
}
